// Fill these in once the GitHub repo exists, then push.
// They point the page at runs/latest_run.json in your own repo via
// raw.githubusercontent.com (which sends CORS headers, unlike Google's
// spreadsheet export endpoints).
const REPO_CONFIG = {
  githubUser: "aaeski",
  githubRepo: "nuvo-retention-guild",
  branch: "main",
};
